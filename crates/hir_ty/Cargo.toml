[package]
name = "hir_ty"
version = "0.1.0"
edition = "2021"

[dependencies]
la-arena = { workspace = true }

hir = { path = "../hir" }

[dev-dependencies]
expect-test = { workspace = true }

ast = { path = "../ast" }
parser = { path = "../parser" }
